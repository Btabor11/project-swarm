# Task

Fill from the person's goal before dispatch; do not invent missing decisions.

- Goal:
- Done-when (observable acceptance):
- Scope / out of scope:
- Providers allowed to receive project code (required):
- Spend ceiling and currency (required):
- Model access verified by smoke runs:
- Acceptance commands / UI or platform checks:
- Shared contract path and agreed names:
- Jobs: deliverable, model, tier, tierReason, context, outputs, tests, dependencies.
- Prior-art report and adoption decisions:
- Current run IDs, ownership, build dispatch count and next action:
- Actual checks, mutations, cost, remaining unknowns:

Keep current after every dispatch. A passing test is evidence only for the
behavior it exercises; a proposed command is not an executed check.
